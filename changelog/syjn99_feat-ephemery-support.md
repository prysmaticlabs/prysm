### Added

- `--ephemery` flag to run on the Ephemery test network. The chain config, bootnodes and genesis state of the running iteration are fetched from https://ephemery.dev/latest at startup, because Ephemery re-genesises every reset period.
