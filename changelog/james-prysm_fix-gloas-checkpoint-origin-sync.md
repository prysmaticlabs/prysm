### Fixed

- Let Gloas checkpoint sync proceed when the origin payload is withheld, preserving and checking origin data columns when a child builds on its FULL status. Accept origin envelopes against checkpoint states advanced through skipped slots.
