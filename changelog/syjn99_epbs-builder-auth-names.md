### Changed

- The config spec key `DOMAIN_REQUEST_AUTH` is now `DOMAIN_BUILDER_REQUEST_AUTH`, in both the `/eth/v1/config/spec` response and custom config YAML files.

### Ignored

- Renamed Gloas builder request authentication types, domain, and data-size constants to match ethereum/builder-specs#165.
