### Fixed

- Fork-choice spectests decode `attester_slashing` steps as `AttesterSlashingElectra` from Electra on, and the spectest chain service is given the slashing, voluntary exit and BLS-to-execution pools that blocks prune after a slashing is inserted.
