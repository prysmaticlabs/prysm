### Changed

- Renamed Gloas builder API request auth and preferences types to their unversioned spec names, and the bid request now requires the signed request auth body and always sends `Date-Milliseconds` and `X-Timeout-Ms`.
