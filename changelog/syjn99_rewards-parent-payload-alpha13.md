### Fixed

- Report the Gloas head-flag proposer reward in block rewards when an included attestation votes for a full parent payload, by applying the parent execution payload before computing rewards.
